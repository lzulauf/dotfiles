#compdef client

_client () {
  # Client dirs under ~/code, excluding the 'worktrees' storage sibling.
  local -a clients
  clients=( ${HOME}/code/*(/N:t) )
  clients=( ${clients:#worktrees} )
  _describe 'client' clients
}
_client "$@"
